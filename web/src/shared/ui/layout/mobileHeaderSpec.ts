/** Figma node 2083:15337 — mobile header (solid pages, e.g. wishlist) */
export const mobileHeaderSpec = {
  height: 64,
  topPadding: 8,
  paddingX: 16,
  sideWidth: 120,
  iconSize: 24,
  iconGap: 24,
  logoWidth: 80,
  logoHeight: 64,
} as const;
