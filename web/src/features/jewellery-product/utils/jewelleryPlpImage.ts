/** Max product card images to load eagerly on the jewellery PLP (above-the-fold). */
export const PLP_PRIORITY_IMAGE_COUNT = 4;

/** next/image quality for PLP product thumbnails — balances sharpness and bytes. */
export const PLP_CARD_IMAGE_QUALITY = 75;

/** Display width for PLP card product shots (matches md:size-[303px]). */
export const PLP_CARD_IMAGE_WIDTH = 303;

/** Hero banner quality — lower than PDP to reduce contention with product LCP. */
export const PLP_HERO_IMAGE_QUALITY = 75;
